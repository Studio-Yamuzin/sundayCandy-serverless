{
    "version": "2017-02-28",
    "operation": "Invoke",
    "payload": {
        "field": "likeContemplation",
        "userId": "$util.defaultIfNullOrBlank(${context.identity.cognitoIdentityId},"test")",
        "arguments":  $utils.toJson($context.arguments),
        "headers": $utils.toJson($context.request.headers)
    }
}